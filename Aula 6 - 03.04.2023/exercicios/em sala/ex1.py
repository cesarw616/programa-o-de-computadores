import os, math
os.system('cls')
fixo=float(input('Digite o valor do seu salário fixo: '))
venda=float(input('Digite o total de suas vendas: '))
print(f'O seu salário desse mês será de R${fixo+(venda*0.04)}')

input('enter para continuar...')